package ci.cryptoneo.signtrust.app.filter;

/**
 * Disabled — caused interference with OIDC authentication.
 * API key auth is handled by ApiKeyAuthFilter (JAX-RS filter) instead.
 */
public class ApiKeyAuthMechanism {
    // Disabled
}
